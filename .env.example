# Copy backend/.env.example to backend/.env and frontend/.env.example to frontend/.env
# Backend: PORT, MONGODB_URI, JWT_SECRET, NODE_ENV
# Frontend: VITE_API_BASE_URL (optional; dev proxy uses http://localhost:5000/api)
